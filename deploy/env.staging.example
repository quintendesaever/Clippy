# Staging runtime secrets — DO NOT commit real values.
# Copy to /data/deployments/clippy/staging/.env (mode 0600).
# NEVER reuse production Discord/Supabase/session secrets.

DISCORD_TOKEN=REPLACE_WITH_STAGING_BOT_TOKEN
CLIENT_ID=REPLACE_WITH_STAGING_APPLICATION_ID
GUILD_ID=REPLACE_WITH_STAGING_GUILD_ID
SUPABASE_URL=https://REPLACE.supabase.co
SUPABASE_SERVICE_ROLE_KEY=REPLACE_WITH_STAGING_OR_ISOLATED_KEY
CLIENT_SECRET=REPLACE_WITH_STAGING_OAUTH_SECRET
SESSION_SECRET=REPLACE_WITH_RANDOM_STAGING_SESSION_SECRET
DASHBOARD_URL=http://100.66.108.109:3100
DASHBOARD_PORT=3000

# No CLOUDFLARE_TUNNEL_TOKEN on staging (private Tailscale bind only).
